{
  "name": "heries",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "SEE LICENSE IN LICENSE",
  "homepage": "https://hongdosan.github.io/heries/",
  "scripts": {
    "dev": "vite",
    "dev:author": "VITE_AUTHOR_MODE=true vite",
    "build": "node scripts/check-images.mjs && tsc -b && vite build && node scripts/copy-content.mjs dist && cp .nojekyll dist/.nojekyll",
    "build:author": "node scripts/check-images.mjs && VITE_AUTHOR_MODE=true tsc -b && VITE_AUTHOR_MODE=true vite build --outDir dist-author && VITE_AUTHOR_MODE=true node scripts/copy-content.mjs dist-author && cp .nojekyll dist-author/.nojekyll",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "optimize:images": "node scripts/optimize-images.mjs"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-router-dom": "^7.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
