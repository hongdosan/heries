# OS
.DS_Store
Thumbs.db

# IDE — IntelliJ IDEA
.idea/
*.iml

# Editor
*.swp
*~

# Harness intermediate artifacts
_workspace/

# Claude Code agent worktrees (ephemeral isolation copies)
.claude/worktrees/

# Serena MCP workspace state (machine-local: indexes, cache, memories)
.serena/

# TypeScript build cache
*.tsbuildinfo

# Node / Vite
node_modules/
dist/
dist-author/
.vite/

# Storybook static build (dev tool — no impact on dist)
storybook-static/

# Environment
.env
.env.local
.env.*.local

# Archived legacy content — replaced by 홍도산-original creator-only work
# (2026-05-12 — removed from public git history via filter-repo; kept locally
#  under content/_legacy_* for author reference only. New original work goes
#  under content/series/clash-of-multiverses/ with the same slug but 100%
#  original characters and worldbuilding.)
content/_legacy_*/

/secret-dir/

# 서브모듈 (.private-config) 으로 옮긴 자료의 심링크 — git tracking X.
# 본 심링크는 scripts/init-private.sh 가 로컬에서 재생성.
# 절차: .claude/harness/private-config.md
.claude/handoff
.claude/harness/harness-state.md
.claude/workflow/plan
.claude/workflow/prompt/custom

# IDE 스크린샷 임시 파일 (작가 보고용)
content/img*.png
