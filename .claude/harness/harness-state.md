<!-- © 2026 hongdosan. All rights reserved. Original creator work. -->

# heries 하네스 — 현 상태

`heries` 의 AI 협업 구조를 정의하는 메타 레이어 문서. 사실 기반으로 현 상태를 누적 기록한다.

## 목차

- [정의](#정의)
- [도메인](#도메인)
- [구성 요소](#구성-요소)
- [에이전트 인벤토리](#에이전트-인벤토리)
- [SSOT 구조](#ssot-구조)
- [자동 강제 메커니즘](#자동-강제-메커니즘)
- [진화 우선순위](#진화-우선순위)
- [변경 이력](#변경-이력)
- [참고](#참고)

## 정의

`heries` 하네스는 자체 런타임 없이 [Claude Code](https://claude.com/claude-code) 위에 **문서 기반 프로세스 하네스 (Documentation-Driven Process Harness)** 를 얹은 구조다.

| 층위 | 정의 | 본 프로젝트 |
|---|---|---|
| Runtime Harness | LLM 호출·도구 디스패치·실행 흐름을 코드로 묶은 시스템 | Claude Code (재사용) |
| Process / Documentation Harness | 페르소나·SSOT·산출물 형식을 문서로 강제하는 메타 레이어 | **본 프로젝트가 정의** |

## 도메인

| 항목 | 값 |
|---|---|
| 서비스 | `heries` — 단일 작가의 소설 컬렉션 정적 사이트 |
| 작품 #1 | **차원의 격돌** (`series/clash-of-multiverses/`) |
| 콘텐츠 성격 | **비상업적 크로스팬픽** — 원작 캐릭터 권리는 각 원저작자에게 귀속 |
| 작성자 | 단일 (`hongdosan`) — 외부 협업자·다인 작가 가정 없음 |
| 독자 | 읽기 전용 (방문자는 수정 권한 없음) |
| 빌더 | 라이브러리 의존성 0 — markdown / HTML 만. GitHub Pages 의 raw static 모드(`.nojekyll`) 또는 정적 호스팅 |
| 배포 | GitHub **공개** 저장소 |

## 구성 요소

`heries` 하네스를 구성하는 요소.

| # | 요소 | 위치 |
|---|---|---|
| 1 | 페르소나/역할 정의 | 각 에이전트 frontmatter + 본문 §0 |
| 2 | 다중 전문 에이전트 분리 | `.claude/agents/agent-{lorekeeper,author,continuity-reviewer,publisher}.md` |
| 3 | 단일 기준점 (SSOT) | 등장인물·세계관·연표·용어집 — `content/series/{slug}/{characters,worldbuilding,timeline,glossary}/` |
| 4 | 단계별 작업 사이클 | 챕터 초안 → SSOT 갱신 → 연속성 감사 → 커밋 → 발행 ([harness-setup.md §6](harness-setup.md#6-작업-사이클)) |
| 5 | AI 자가 검증 | `agent-continuity-reviewer` 가 신규 챕터의 SSOT 정합성 감사 |
| 6 | 산출물 스키마 | 등장인물 카드 frontmatter (`name`, `origin`, `affiliation`, `role`, `first_appearance`), 챕터 frontmatter (`title`, `episode`, `published`) |
| 7 | 비상업적 팬픽 고지 | 모든 .md 첫 줄 HTML 주석 1줄 — CLAUDE.md 에 강제 |
| 8 | 도구 우선순위 | `.claude/CLAUDE.md` §도구 우선순위 + Serena MCP 사용 지침 |
| 9 | 변경 이력 (Synchronous Update) | 본 문서 §변경 이력 — 하네스 docs 3종의 SSOT |

## 에이전트 인벤토리

2026-05-12 하네스 엔지니어링: 4 → 6 세분화. 코드 작업 분리 + 세계관 분리.

| 에이전트 (frontmatter `name`) | 역할 | 상태 | 정의 |
|---|---|---|---|
| `heries-lorekeeper` | 캐릭터 카드 SSOT (`characters/`) 작성·정정·검증 | 작성 (2026-05-12) | [`../agents/heries-lorekeeper.md`](../agents/heries-lorekeeper.md) |
| `heries-worldsmith` | 시리즈 메타 + 세계관·연표·용어집 (`_series.md`, `worldbuilding/`, `timeline/`, `glossary/`) | 작성 (2026-05-12, 신규) | [`../agents/heries-worldsmith.md`](../agents/heries-worldsmith.md) |
| `heries-author` | 챕터 본문 작성 (`chapters/`) — 시놉시스 사실화 / 플롯 보존 | 작성 (2026-05-12) | [`../agents/heries-author.md`](../agents/heries-author.md) |
| `heries-continuity-reviewer` | 신규 챕터 정합성 감사 (직접 수정 X — 보고서만) | 작성 (2026-05-12) | [`../agents/heries-continuity-reviewer.md`](../agents/heries-continuity-reviewer.md) |
| `heries-frontend-engineer` | `src/` (FSD) + `scripts/` + 빌드 설정. Serena MCP 우선, 의존성 0 정책 | 작성 (2026-05-12, 신규) | [`../agents/heries-frontend-engineer.md`](../agents/heries-frontend-engineer.md) |
| `heries-publisher` | 발행·배포 — manifest, 빌드 검증, 이미지 강제, 썸네일 프롬프트, GH Pages | 작성 (2026-05-12) | [`../agents/heries-publisher.md`](../agents/heries-publisher.md) |

작성률: **6/6 (100%)**. 라우터 = [`../skills/heries-orchestrator/SKILL.md`](../skills/heries-orchestrator/SKILL.md).

## SSOT 구조

```
content/
  series/
    clash-of-multiverses/        # 작품 #1: 차원의 격돌
      _series.md                 # 시리즈 메타 (제목·시놉시스·연재 상태·차용 원작 목록)
      characters/                # 등장인물 SSOT (캐릭터당 1 파일)
        {character-id}.md        # frontmatter + 서술 본문
      worldbuilding/             # 지역·세력·체계
      timeline/                  # 연표
      glossary/                  # 용어집
      chapters/
        ep-01-{slug}.md          # 에피소드 본문 (frontmatter + 본문)
        ep-02-{slug}.md
        ...
```

### 등장인물 카드 frontmatter 예시

```markdown
---
name: 캐릭터 표시명
origin: 원작 출처 (예: "OOO 웹툰 - 작가명") — 차용 시 필수, 누락 시 reject
affiliation: 소속 (조직/세력/소속 시리즈)
role: protagonist | antagonist | supporting | cameo | original
first_appearance: ep-01-prologue
aliases: [별칭1, 별칭2]
heries_arc: tba   # 본 작품에서의 소환 시점·상태. 보류 시 tba (선택 필드)
---

(본문 = 원작 메타 → 원작 캐논 → heries 분기 → 검증 출처 4 절 구조)
```

> `origin` 필드는 **차용 캐릭터** 식별에 필수. 본 작품 오리지널 캐릭터는 `origin: original`.
> *다중 우주 전제* 로 인해 본문은 *원작 캐논 절* 과 *heries 분기 절* 을 분리 작성한다. 정책 SSOT: [`../../content/series/clash-of-multiverses/_series.md`](../../content/series/clash-of-multiverses/_series.md) §기본 전제.

## 자동 강제 메커니즘

| 항목 | 존재 |
|---|---|
| `heries-continuity-reviewer` 자동 호출 (신규 챕터 작성 시) | **구현** — `heries-orchestrator` SKILL.md §4-2 챕터 작성 파이프라인 (author → continuity-reviewer 자동) |
| 등장인물 카드 frontmatter 스키마 검증 | LLM 순응 (`heries-lorekeeper` §검증 체크리스트) |
| 챕터 → 캐릭터 참조 무결성 검증 | LLM 순응 (`heries-continuity-reviewer` §검증 체크리스트) |
| 비상업적 팬픽 고지 부착 강제 | **구현** — CLAUDE.md §핵심 원칙 #1 + 모든 에이전트 §작업 원칙 |
| 이미지 budget 강제 (≤500KB) | **구현** — `scripts/check-images.mjs` 빌드 게이트 (build·build:author 시작에 chain) |
| 작가 빌드 라이브 노출 차단 | **구현** — `.github/workflows/deploy.yml` 의 `VITE_AUTHOR_MODE: ""` env 강제 |
| Pre-commit hook | 없음 (사용자 메모리 = 사용자 직접 commit 정책 — hook 도입 보류) |

자동 강제: **4건** (orchestrator 파이프라인 + 고지 부착 + 이미지 budget + 작가 빌드 격리). 콘텐츠 정합성은 LLM 순응 + 에이전트 §검증 체크리스트.

## 진화 우선순위

1. **`agent-lorekeeper` 시범 생성** ([harness-install.md §4](harness-install.md#4-첫-적용--agent-lorekeeper-시범-생성)) — harness Phase 1–6 자동 실행. 가장 핵심: 등장인물 SSOT 정의·검증.
2. **첫 시리즈 메타 작성** — `content/series/clash-of-multiverses/_series.md` (시놉시스 + 차용 원작 목록).
3. **첫 챕터 시범 작성 + `agent-author` 도입** — 한 사이클 완주 (집필 → SSOT 갱신).
4. **`agent-continuity-reviewer` 도입** — 두 번째 챕터 진입 직전. 첫 챕터를 바탕으로 검증 규칙 학습.
5. **`agent-publisher` 도입** — GitHub Pages 발행 절차 자동화. `.nojekyll` 라우팅·인덱스 페이지 갱신·`gh` CLI 래퍼.
6. **자동 강제 도입 (선택)** — frontmatter 스키마 검증 스크립트 (라이브러리 미사용 — bash + grep 등). pre-commit 도입 여부는 사용자 판단.

## 변경 이력

harness Phase 7 패턴을 차용한 변경 이력. 모든 진화 변경은 *날짜 / 변경 내용 / 대상 / 사유* 4컬럼으로 기록.

> **Hot 한도: 20행.** 21행 도달 시 **hot 에 최근 3행만 남도록 가장 오래된 행을 한꺼번에** [`harness-state-archive.md`](harness-state-archive.md) 로 이동 (= 매번 약 18행 일괄 이동, **1줄 bullet 압축 형식** `- YYYY-MM-DD: 핵심`). 카드 1장당 발동 사이클 회피. 원본 복원은 git history. 정책 SSOT: [`../CLAUDE.md`](../CLAUDE.md) §누적 산출물. 점검 시점 = 세션 시작 직후 + 세션 종료 직전.

| 날짜 | 변경 내용 | 대상 | 사유 |
|---|---|---|---|
| 2026-05-11 | **GitHub Pages 자동 배포 워크플로우 추가 (main 트리거)** — 사용자 결정 = *main 에서 다 진행* + *배포 규격 엄격하게 X*. 참고 워크플로우 (`release` 브랜치) 의 *main* 트리거 변형. `.github/workflows/deploy.yml` 신규: (a) `on.push.branches: ["main"]` + workflow_dispatch 수동 (b) permissions = contents:read + pages:write + id-token:write (c) concurrency group "pages" cancel-in-progress:false (d) build job = checkout v4 + setup-node v4 (Node 20, npm cache) + `npm ci` + `npm run build` + upload-pages-artifact@v3 path './dist' (e) deploy job = deploy-pages@v4 + environment github-pages. **안전장치**: build job env `VITE_AUTHOR_MODE: ""` 명시 — 작가 빌드가 자동 배포 경로에 절대 들어갈 수 없음. **`.nojekyll` 빌드 자동 복사** = `package.json` build/build:author 스크립트 끝에 `cp .nojekyll dist/.nojekyll` 추가 (Jekyll 의 `_` 접두사 무시 방지 — `_series.md` 가 dist 에 들어가 있으므로 필수). 검증: rm -rf dist + npm run build → dist/.nojekyll (0 byte) 정상 생성 + dist 루트 4 항목 (.nojekyll/assets/content/index.html). 사용자 액션 필요: GitHub Repository → Settings → Pages → Source = GitHub Actions 변경 (1회) | `.github/workflows/deploy.yml` (신규), `package.json` (build/build:author scripts 에 .nojekyll 복사 추가), `README.md` (§배포 절 추가) | 본 시리즈 첫 *자동 배포 파이프라인*. 참고 프로젝트 (martial-arts) 의 워크플로우를 main 트리거 + reader 모드 강제로 변형. 작가 빌드 보호 = (1) build:author 가 dist-author/ 별도 디렉토리 (2) 워크플로우 env 가 VITE_AUTHOR_MODE="" 강제 — 두 단계 안전. main 단일 브랜치 운용 = 사용자 메모리 정책 (워크트리 X / 사용자 직접 commit) 와 정합. 변경 이력 hot 18행 / 한도 20행 — 여유 2 |

| 2026-05-11 | **1·2화 서사 보강 — *왜?* 결핍 4점 해소 + 페이즈 2 빌드업 떡밥 매설** — 사용자 진단 = *각 주인공 갑자기 부르고 냅다 소개 후 싸우기 시작·왜? 가 없음·서사가 탄탄해 독자 납득하면 함*. 진단 4점: (1) 소환 *순간* 부재 — 폐허 한복판에서 즉시 시작 (2) 운명 각인의 *정체* 부재 (누가 새겼는지 미공개) (3) 첫 *움직임 방아쇠* 부재 (1화 콰광 → 2화 나루토 분신술 점프) (4) 인물 *감정 곡선* 빈약 (대부분 이미 받아들인 자세). 보강 4 영역: (a) **1화 도입 신규 단락** — 14인 각자 직전 일상 (성진우=차/청명=매화/카디스=창가/광마=호리병/카이든=사무실/밤=탑/박형석=옥상라면/루피=사니호/김독자=책/검무극=명상/임강=산책/진모리=라그나로크 구름/나루토=호카게서류/백수룡=강의실분필) → 흰빛 → 눈을 뜨니 폐허. 14명 모두 *원래 무엇을 하다 끌려왔는지* 1구절씩. (b) **운명 각인의 정체 = *글자*** — 김독자가 가장 먼저 *시야 위로 흘러내리는 글* 인지 (제4의 벽 미세하게 떨림). [전지적 독자 시점] 의 텍스트도 멸살법 문체도 아닌 *지금 이 장면을 쓰고 있는 듯한 글*. *이건 시놉시스다. 누군가가 이 자리를 쓰고 있다* — 페이즈 2 *작가 토벌* 떡밥 직접 매설. 박형석의 *왜 우리가……* 첫 인간적 저항 + 김독자의 *답을 알기 위해서라도 살아남아야 한다* 가 *왜 싸우는가* 의 답. (c) **1화 끝 — 카운트 = 두 번째 글 *시작*** — 단 두 글자 시야에 새겨짐 + 운명이 *호흡* 에서 *충동* 으로 변환 + 가장 먼저 받아들인 자 = 광마 (광증의 정체성과 일관). (d) **2화 도입 인과 매끈화** — 광마 첫 살의 (박형석 표적) → 나루토 분신이 가로막아 박형석 살림 → 다른 분신들 사방 흩어짐 → 청명 매화만개 청소 의 인과 명시. 광마의 본격 등판 (82행) 부분도 *박형석을 노리던 첫 일격은 몸풀기* 톤 조정. 김독자 (134행) 부분에 두 번째 *낯선 글* 회수 (*살아남는 자가 끝이 아니라 시작* — 페이즈 1 → 2 전환 떡밥), 나루토 분신 부분에 *살릴 수 있는 자는 살린다* 호카게 본능 1줄 (*왜 영문 모르는 누군가가 이 자리에서 죽어야 하는지도 모른다 — 그것이 이유의 전부* = 인물 동기 명시). 챕터 작성 워크플로우 정책 (시놉시스 사건·결과 보존) 준수: *14명 → 11명 1막 추락 결과* + *광마/박형석/카디스 추락 종결자* + *시놉시스 골격 (작가 = 진범)* 모두 보존, 변경 없음. 카드 SSOT (광마 = 광증 = 가장 먼저 못 참는 캐릭터 / 김독자 = [전지적 독자 시점] = 시점 메타 / 박형석 = 무의식 = 평범인 감정 노출) 활용. 검증: typecheck 0 / build 477ms 0 에러 / dist ep-01 신규 도입 14명 일상 1구절씩 정상 반영 | `content/series/clash-of-multiverses/chapters/{ep-01-prologue,ep-02-first-fall}.md` (1화 ~3 단락 추가 / 2화 ~4 단락 추가·조정) | 본 시리즈 첫 *서사 떡밥 매설* 사이클. 작가 = 시놉시스를 쓰는 자 = 페이즈 2 진범 의 메타 모티프가 *김독자 = 글을 읽는 자* 와 정확히 결합 → 페이즈 2 도입 시 김독자가 가장 먼저 *작가의 정체* 에 접근하는 동선 자연스러움. *시작* 글자 카운트 = 페이즈 1 발동의 작가 신호 = 다음 화 *끝* 또는 *다음* 등 카운트 글자 추가 가능. 변경 이력 hot 19행 / 한도 20행 — 여유 1, 다음 사이클 시작 직후 압축 임계 임박 보고 필요 |

| 2026-05-11 | **썸네일 시스템 추가 (작품 + 챕터) + AI 생성 프롬프트 가이드** — 사용자 결정 = *썸네일 추가 / 각 작품 + 각 챕터마다 / 이미지는 사용자 직접 제공 / 프롬프트는 Claude 제공*. 데이터 스키마: `SeriesIndex`·`SeriesManifest`·`ChapterIndex` 인터페이스에 `thumbnail?: string` 옵셔널 필드 추가. 파일 경로 규약 = `content/series/{slug}/thumbnails/{cover|ep-NN-{slug}}.webp` (16:9 1600×900 권장). manifest.json + series.json 의 시리즈 + 챕터 항목에 placeholder 경로 등록. UI 노출 4 위치: (1) **홈 시리즈 카드** = 카드 상단 16:9 썸네일 + 호버 시 scale(1.03) (2) **시리즈 hero** = cover 가 있으면 `has-cover` 클래스로 grid 280px / 1fr 분할 레이아웃, 모바일에서 단일 컬럼 (3) **챕터 목차 행** = 좌측 96×54 썸네일 + EP 태그 + 제목 + 발행일 4-컬럼 grid (4) **챕터 페이지** = 본문 위 16:9 cover figure. 모든 위치 옵셔널 — 썸네일 없으면 *대각선 빗금 패턴 placeholder* 로 우아한 fallback (`linear-gradient` + `repeating-linear-gradient` CSS only). 빌드 시 스포 보호 보강: `copy-content.mjs` 의 `walk` 함수에 *thumbnails/ 내의 .md 파일 (예: PROMPTS.md) 은 reader 빌드 제외* 규칙 추가 — 프롬프트가 일부 스포 (1막 3추락 결과 등) 를 포함해 reader 노출 불가. AI 생성 프롬프트 (3개) = (a) cover = 14인 다중우주 배틀로얄 키 아트, 16:9 와이드, 안개 폐허 + 14 실루엣 + 상단 floating 글리프 리본 (b) ep-01-prologue = 14명이 막 깨어난 폐허, 핏빛 fog + 깨어진 달 + subtitle 글 (c) ep-02-first-fall = 격돌 직후 잔재, 3 추락 silhouette + 5색 기운 ember + 피닉스 날개 잔영 + 11 backlit 실루엣. 모든 프롬프트 negative = readable text + character close-up + watermark + IP costume identifiable. 작가 = WebP 1600×900 200KB 이하 압축 후 commit. 검증: typecheck 0 / reader build = 27 masked (dist/content/thumbnails 디렉토리 자체 미생성 — 이미지 없는 빈 디렉토리 자연 스킵) / author build = 33 verbatim (PROMPTS.md 포함) | `content/series.json` (thumbnail 1행), `content/series/clash-of-multiverses/manifest.json` (thumbnail + chapters[].thumbnail 3행), `content/series/clash-of-multiverses/thumbnails/PROMPTS.md` (신규), `src/shared/lib/types.ts` (SeriesIndex·SeriesManifest·ChapterIndex 옵셔널 필드), `src/widgets/{series-list,chapter-toc}/*.tsx` (썸네일 마크업), `src/pages/{series,chapter}/*.tsx` (hero 썸네일), `src/shared/styles/style.css` (series-card 재구조화 + chapter-row grid + series-hero has-cover + chapter-cover + placeholder 빗금 패턴 + 모바일 적응), `scripts/copy-content.mjs` (thumbnails/*.md reader 스킵 규칙) | 본 시리즈 첫 *시각 자산 시스템*. 옵셔널 설계 — 이미지 0장 상태에서도 사이트 동작 OK (placeholder fallback). 작가 (`hongdosan`) 가 이미지 3장 추가하면 즉시 visual 강화. AI 생성 프롬프트 표준화 = 향후 3화·4화 추가 시 동일 톤·비율·negative 패턴으로 일관성. 스포 보호 = thumbnails/ 내 .md 작가 메타가 빌드 시 자동 제거되어 라이브 노출 X. **변경 이력 hot 20행 / 한도 20행 — 임계 도달**. 다음 세션 시작 직후 21행 도달 시 일괄 archive 압축 발동 (정책 SSOT: CLAUDE.md §누적 산출물) |
| 2026-05-11 | **10차 archive 압축 발동 — hot 21행 도달 직전 17행 일괄 이동** — 직전 사이클 (썸네일 시스템) 행 추가로 hot 20행 임계 도달 직후, 본 사이클 (이미지 압축 강제화) 추가 시점에 정책 SSOT 트리거 발동. 17행 → `harness-state-archive.md` bullet 형식 (`- YYYY-MM-DD: 핵심 1줄`) 일괄 이동. hot 표는 최근 3행 (GitHub Pages 자동 배포 / 1·2화 서사 보강 / 썸네일 시스템) 만 보존. 표 헤더 구분자 (`\|---\|---\|---\|---\|`) 누락된 것 함께 정상화. archive 누적 행 = 33개 (이전 16 + 신규 17). 원본 4컬럼 (대상·사유) 복원이 필요하면 git log 로 해당 행이 hot 에 있던 commit 참조 | `.claude/harness/harness-state.md` (hot 표 정리), `.claude/harness/harness-state-archive.md` (17 bullet 누적) | 정책 v2 (2026-05-10 정의) *9차 압축 이후 첫 본격 운용 사이클* 정상 동작 검증. 카드 1장당 archive 트리거 발동 회피 (이전 정책 대비) + bullet 압축으로 archive 무게 경량 유지. 본 행 = 메타-기록 (압축 자체를 변경 이력에 1행으로 명시 — 정책 강제) |
| 2026-05-11 | **`_series.md` 운영 디테일 노출 정리 + CLAUDE.md 카드 구조 SSOT 규약 명시** — 사용자 진단 = `_series.md` §기본 전제 의 *frontmatter `heries_arc` 필드에 기록, 보류 시 `tba`* + *원작 캐논 절·heries 분기 절 분리 작성* 두 항목이 reader 노출 문서에 들어가 있는 SSOT 구현 디테일 (필드명·placeholder·절 이름) — 독자는 알 필요 없음. 정리 대상 2건: (1) line 20 의 *frontmatter `heries_arc` … 보류 시 `tba`* 절단 → *어떤 시점의 캐릭터가 본 작품 무대에 등장하는지는 작가가 결정한다.* 로 축약 (서사 사실만 보존). (2) line 21 의 *원작 캐논 ↔ 본 작품 변형의 분리 표기* 항목 전체 제거 (line 18-19 의 *원작 캐논 출발점 + 평행 우주 변형 가능* 과 의미 중복). 동시에 CLAUDE.md 원칙 #2 (origin reject) 에 하위 3개 불릿 추가: (a) 카드 구조 SSOT = 원작 캐논 절 vs heries 분기 절 분리 작성, 변형은 후자에만 기재 (b) `heries_arc` 필드 = 소환 시점 기록 (예: `① E급 헌터 시기` / 보류 시 `tba`), 원칙 #9 마스킹 대상 (c) 운영 디테일 미노출 = reader 노출 문서 본문은 서사 사실만, 구현 디테일 (필드명·placeholder·절 구조) 노출 금지. 효과 = `_series.md` reader 표시 깔끔 (백틱 슬러그 노이즈 제거) + SSOT 운영 규약은 CLAUDE.md 단일 위치로 통합 | `content/series/clash-of-multiverses/_series.md` (line 20 축약 + line 21 제거), `.claude/CLAUDE.md` (원칙 #2 에 3 하위 불릿 추가) | 본 시리즈 첫 *reader 노출 문서 ↔ SSOT 운영 규약 분리* 사이클. 사용자 메모리 *SSOT 골격만, 디테일은 챕터 중 점진 동기화* 정책과 정합 — 운영 규약은 코드/메타 영역, reader 본문은 서사 영역. 향후 새 시리즈 추가 시 동일 패턴 = `_series.md` 는 서사 / CLAUDE.md 는 SSOT 규약 |
| 2026-05-11 | **캐릭터 페이지 frontmatter 필드 inline 마크다운 렌더링** — 사용자 스크린샷 제보 = 진모리 카드 사이드바 `원작` 필드에 `**박용제**` · `*쎈놈*` · `**2011-04-08…**` 등 raw 마크다운 노출. 원인 = `src/pages/character/character.tsx` 의 `<dd>{fm.origin}</dd>` 가 escaping 만 하고 렌더링 안 함. frontmatter 의 origin/affiliation 필드는 풍부한 마크다운 (bold·italic·링크) 을 포함하지만 사이드바는 inline-only 컨텍스트이므로 `<p>` 래퍼 있는 `renderMarkdown` 부적합 → `renderInline` 을 export 하고 `dangerouslySetInnerHTML` 로 적용. 적용 필드 = `원작 (origin)` + `소속 (affiliation)` + `이명 (aliases)` 각 항목. `첫 등장 (first_appearance)` = 슬러그형 ID 라 plain 유지. 검증: 11개 inline 케이스 + 실제 진모리 origin 라인 dry-render → `<strong>박용제</strong>` 정상 변환. typecheck 0 | `src/shared/lib/markdown.ts` (`renderInline` export 1줄), `src/pages/character/character.tsx` (import + dd 3개 dangerouslySetInnerHTML 전환) | 본 시리즈 첫 *frontmatter 마크다운 렌더링* 적용. 22 캐릭터 카드 전부 수혜 (origin 필드는 거의 모든 카드에 bold/italic 포함). 향후 frontmatter 에 마크다운 들어가는 다른 필드 추가 시 동일 패턴 |
| 2026-05-11 | **마크다운 렌더러 깨짐 패턴 3종 수정** — 사용자 진단 = 6 캐릭터 카드 (sung-jin-woo / geom-mu-geuk / jin-mori / kayden-break / lee-ja-ha / park-hyeong-seok) 화면 깨짐. 원인 = `src/shared/lib/markdown.ts` 의 한정된 패턴 매칭. 3개 패턴 식별: (1) **bold containing italic** — `**A *B***`, `***foo***`, `**A *B* C**` — 기존 bold regex `\*\*([^*\n]+?)\*\*` 가 `*` 를 content 에서 배제해 6개 파일 전반의 트레이드마크 대사 (lee-ja-ha 광마 대사 12개·jin-mori 본명 강조·geom-mu-geuk 절대마신 등) 가 literal `*` 로 노출. 수정 = `\*\*([^\n]+?)\*\*(?!\*)` — `*` 허용 + close lookahead. (2) **lists inside blockquotes** — `>` 핸들러가 `buf.join(' ')` 로 평탄화하여 `> - item1\n> - item2` 가 한 줄로 합쳐짐 (4 파일의 §출처/미상 절). 수정 = `<blockquote>${renderMarkdown(buf.join('\n'))}</blockquote>` 재귀 호출. (3) **nested lists** — 4 space 들여쓰기 sub-bullet (sung-jin-woo 의 8개 소환 시점 후보 등 5+ 파일) 이 단일 평탄 `<ul>` 로 합쳐짐. 수정 = 신규 `renderNestedList` 헬퍼로 indent stack 기반 hierarchical `<ul><li>…<ul>…</ul></li>` 빌드. 검증: 11 케이스 (`**simple**` ~ `**A *B* C**` ~ 실제 파일 라인 ~ table cell ~ nested list) 모두 renderMarkdown 정상 출력 + 6 파일 전체 dry-render 시 literal `*` leftover = 0건. typecheck 0. | `src/shared/lib/markdown.ts` (bold regex 1줄 + blockquote 1줄 + 신규 `renderNestedList` 함수 19줄) | 본 시리즈 첫 *코어 렌더러* 수정. 영향 범위 = 6 캐릭터 카드 + 향후 모든 .md (CommonMark 의 `**A *B***` 와 nested list, blockquote-list 는 매우 흔한 패턴 — 다른 카드도 잠재적 수혜). 의존성 0 정책 유지 (커스텀 renderer 단일 파일 자체 수정). 변경 이력 hot 6행 / 한도 20행 — 여유 14 |
| 2026-05-11 | **이미지 압축 자동화 + 빌드 시 강제 게이트 (옵션 A)** — 사용자 결정 = *옵션 A로 진행* + *이미지 압축도 강제화*. 1단계: 사용자 직접 제공 이미지 (cover.webp 3.8MB / thumbnail-placeholder.webp 4.0MB) 가 사양 (200KB 권장) 의 19~20배 초과 발견 → `scripts/optimize-images.mjs` 신규 (Node 표준 + `npx -y sharp-cli@latest` 임시 다운로드, 의존성 0 정책 부합 — @squoosh/cli 는 Node 22 worker_threads 호환 X 라 sharp-cli 채택). 동작 = content/_shared + content/series/*/thumbnails 의 webp/jpg/jpeg/png 일괄 → WebP quality 80 + max width 1600 + 임시 디렉토리 staging 후 원자 swap (실패 시 원본 보존). 실측: 7705KB → 422KB (-95%, 2 파일). 2단계: **빌드 시 강제 게이트** — `scripts/check-images.mjs` 신규 (Node 표준 fs only). 한도 = 500KB/이미지. 빌드 (`npm run build` + `npm run build:author`) 시작에 `node scripts/check-images.mjs` chain → 초과 시 빌드 실패 + 친절 안내 (*fix: npm run optimize:images*). 검증: 통과 case = `✓ image budget OK (2 files, all ≤ 500 KB)`, 합성 1MB 파일 case = exit 1 + 안내 명시. GitHub Actions 의 build job 도 동일 path 거치므로 자동 보호 — 큰 이미지가 라이브 배포되는 경로 차단 | `scripts/optimize-images.mjs` (신규), `scripts/check-images.mjs` (신규), `package.json` (`optimize:images` script + build/build:author 시작에 check-images chain), `content/_shared/thumbnail-placeholder.webp` (4MB → 219KB), `content/series/clash-of-multiverses/thumbnails/cover.webp` (3.7MB → 203KB) | 사용자가 새 이미지 추가 시 운영 절차 = (1) 그냥 디렉토리에 넣음 (해상도/용량 무관) (2) `npm run optimize:images` 1번 실행 → 자동 압축. *압축 잊고 commit 시* = 빌드 실패로 보호. 라이브 배포는 항상 ≤500KB 이미지만. 의존성 0 정책 정합 (npx 임시 다운로드). 본 행 추가로 hot 5행 (압축 메타 + 이미지 강제화 + 직전 3행) — 한도 20 내 여유 15 |
| 2026-05-12 | **`hong` → `hongdosan` 작가 표기 일괄 정정 + 원작 미경험 독자 도움 기능 (챕터 등장인물 strip + 본문 용어 풀이 + 카드 한입 요약) 도입** — 사용자 명시 = *작가는 hongdosan, hong 잔재 정정* + *원작 모르는 독자도 이해하기 쉽게 + 어려운 용어 간단 풀이* + *각 카드가 너무 복잡 — 빠른 요약 제공* + UX 보정 *strip 가로 스크롤 별로 / 좌측 이미지 공간 제거*. 사용자 메모리 정정 = `user_author_name.md` 의 *내부 약칭 hong* 폐기 → 모든 영역 hongdosan. **(1) hong 정정** = `\`hong\`` → `\`hongdosan\`` sed 일괄 (.claude + content + README, 25 파일 100+건) + index.html title/description + CURRENT.md 약칭 라인 정리 + README/harness.md 어원 설명. **(2) 원작 도움 기능** = (a) frontend-engineer agent 위임 (orchestrator 라우팅 첫 검증), (b) 챕터 frontmatter `characters: [slug...]` 옵셔널 배열, (c) 캐릭터 frontmatter `summary?: string` 옵셔널 (1~3줄 한입 요약, 원작 미경험 대상), (d) 신규 위젯 `src/widgets/chapter-character-strip/` (FSD widget 레이어, 카드 = 이름 + 작품명 + summary, 좌측 이미지 공간 제거, flex-wrap 으로 가로 스크롤 0, viewport 360/768/1024/1440 모두 자연 줄바꿈), (e) 캐릭터 페이지 hero `.character-summary` aside (큰 글자 좌측 강조선, 3초 인지), (f) 마크다운 렌더러 신규 패턴 `{용어\|설명}` → `<abbr title="설명" class="heries-term">용어</abbr>` (sentinel-stash 로 후속 bold/italic regex 침투 방지, CSS 점선 underline + cursor:help, 13 dry-render 케이스 통과). **(3) 시드** = ep-04 본문 2 용어 (`{멸살법\|...}` `{기어 5\|...}`) + 5인 카드 summary (성진우/진모리/임강/검무극/이자하). 검증 = typecheck 0 / reader build 성공 (10 verbatim, 29 masked) / dist 마스킹 누수 0 (heries_arc 0건, heries 분기 절 0건). 주의 사항 = sung-jin-woo origin 디테일 1건 인용 라벨 (*본문 [1] 명시*) 누락 — 외부 린터의 YAML fold + parseYaml 빈 string 회피 위해 single-line quote 복원 시 단순화. 본질 데이터 보존, 다음 사이클 lorekeeper 정밀 재작성 권장. ep-04 characters 배열 = 14인 그대로 유지 (5인 명세였으나 이미 등재된 14인 graceful degradation 으로 유지) | `.claude/CLAUDE.md`, `.claude/handoff/CURRENT.md`, `.claude/harness/{harness,harness-state,harness-state-archive}.md`, `README.md`, `content/series/clash-of-multiverses/{_series.md,thumbnails/PROMPT_GUIDE.md}`, 챕터 4개 + 캐릭터 카드 22개 (sed 일괄), `content/series/clash-of-multiverses/chapters/ep-04-shadow-monarch-rises.md` (abbr 2 시드), 5인 카드 summary 시드, `index.html` (hong 정정 + favicon link), `src/widgets/chapter-character-strip/{chapter-character-strip.tsx,index.ts}` (신규), `src/shared/lib/{types,markdown}.ts`, `src/shared/styles/style.css`, `src/pages/{chapter,character}/*.tsx` | 본 사이클 = (1) heries-orchestrator 라우팅의 *첫 실주행* — frontend-engineer agent 백그라운드 위임 → SendMessage 로 명세 3차 확장 (summary / strip wrap / 좌측 이미지 제거) → 통합 결과 보고. 위임 패턴 정상 동작 검증. (2) 사용자 메모리 정정 (hong 폐기) 은 향후 모든 신규 산출물의 작가 표기 명확화. (3) summary 필드 도입은 22 카드 중 5인만 시드 — 작가 직접 또는 lorekeeper 백필. (4) `{용어\|설명}` 패턴은 작가가 향후 챕터 작성 시 직접 표기 (자동 추출 X). (5) ep-04 등장인물 strip 은 ep-01~03 백필 시 동일 효과. (6) 다음 사이클 후보: ep-01~03 characters 배열 백필 / 22 카드 summary 백필 / sung-jin-woo origin 정밀 재작성 / 기존 카드 본문의 SSOT 메타 노트 정리 (jang-il-so 등 6 카드). 변경 이력 hot 12행 / 한도 20 — 여유 8 |
| 2026-05-12 | **하네스 엔지니어링 — 6 에이전트 + 1 오케스트레이터 일괄 작성 (zero-state → 100% 작성)** — 사용자 요청 = *에이전트 세분화 + .claude 전체 개선*. harness 스킬 호출. Phase 0 감사 = `.claude/agents/` · `.claude/skills/` · `.claude/commands/` 모두 부재 (zero-state). harness/ 도입 가이드 3종 + state SSOT + handoff 인프라만 보유. **세분화 결정 = 4 → 6** (변경 이력 기반 작업 패턴 분석): (1) 기존 `agent-lorekeeper` (캐릭터) 유지 (2) **신규 `heries-worldsmith`** = `_series.md` + worldbuilding/timeline/glossary 분리 — 다중우주 크로스팬픽 전제로 세계관 복잡 (3) `agent-author` (챕터) 유지 (4) `agent-continuity-reviewer` (정합성) 유지 (5) **신규 `heries-frontend-engineer`** = src/ FSD 6 레이어 + scripts/ + 빌드 설정 분리 — 마크다운 렌더러 버그 수정·UX·마스킹 로직 작업이 publisher 와 별개로 누적 (6) `agent-publisher` (발행) — 코드 영역은 frontend-engineer 로 이전, manifest·빌드 검증·썸네일 프롬프트·이미지 강제·GH Pages 만 유지. **실행 모드 = 서브 에이전트 기본** (단일 작가 도메인이라 팀 통신 오버헤드 회피) **+ 챕터 작성 사이클은 author → continuity-reviewer 파이프라인 (하이브리드)**. **스킬 = `heries-orchestrator` 1개** (라우터 + 사이클 가이드, lean 정책으로 추가 스킬은 반복 패턴 발견 시 분리 — 사용자 메모리 *점진 동기화* 정책 정합). 산출물 = (a) `.claude/agents/heries-{lorekeeper,worldsmith,author,continuity-reviewer,frontend-engineer,publisher}.md` 6 파일 (각 60~120줄, frontmatter `name`+`description`+`model: opus` + 책임/원칙/I·O/협업/검증/트리거 7절 구조) (b) `.claude/skills/heries-orchestrator/SKILL.md` (라우팅 매트릭스 + 챕터 작성 파이프라인 + 사용자 commit 정책 강제) (c) `.claude/CLAUDE.md` §하네스 트리거 표 = 6 에이전트 + 키워드 + 정의 링크로 갱신 + 실행 모드·model:opus·commit 정책 명시 (d) 본 §에이전트 인벤토리 표 = 6 에이전트 100% 작성 갱신. 모든 에이전트 .md 첫 줄 = 비상업적 팬픽 고지 1줄. 에이전트 간 import = 단방향 (orchestrator → 6 에이전트 / lorekeeper ↔ worldsmith ↔ author ↔ continuity-reviewer ↔ publisher / frontend-engineer 는 publisher 와만 협업). harness/ 도입 가이드 3종 (harness.md / setup / install) 은 본 1차 패스에서는 보존 — 다음 사이클에서 README.md 통합 (legacy 마킹 또는 흡수) 검토. 검증 = 트리거 키워드 충돌 0건 / 모든 에이전트 정의 frontmatter 적합 / orchestrator description "pushy" 작성 (반드시 트리거할 것 명시) | `.claude/agents/heries-{lorekeeper,worldsmith,author,continuity-reviewer,frontend-engineer,publisher}.md` (신규 6 파일), `.claude/skills/heries-orchestrator/SKILL.md` (신규), `.claude/CLAUDE.md` (§하네스 트리거 표 6 에이전트로 갱신), `.claude/harness/harness-state.md` (§에이전트 인벤토리 6/6 갱신) | 본 시리즈 첫 *실체 하네스 구축* 사이클 — 1년 가까이 유지된 zero-state (계획만 존재) 종료. 향후 모든 작업이 6 에이전트 라우팅 체계로 진행. 사용자 메모리 정책 (*main 브랜치만 + 사용자 직접 commit / 점진 동기화 / 챕터 = 사실화*) 모두 에이전트 정의 §협업 + §작업 원칙에 명시. Serena MCP (직전 사이클 도입) 는 frontend-engineer §작업 원칙 #4 에 *src/ 코드 탐색 우선* 으로 통합. 다음 사이클 후보 = (1) harness/ 도입 가이드 3종 정리 (legacy 마킹 또는 README 흡수) (2) 첫 실주행 후 진화 트리거 (반복 피드백 2회 발견 시 에이전트/스킬 수정) (3) 추가 스킬 분리 후보 (`chapter-write`, `card-create` — 반복 패턴 검증 후). 변경 이력 hot 11행 / 한도 20 — 여유 9 |
| 2026-05-12 | **Serena MCP 연동 (`serena-heries`) + CLAUDE.md 도구 우선순위 갱신** — 사용자 요청 = *코드 탐색 토큰 효율 개선* 문서 기반 도입. 배경 = `Grep` / 파일 전체 `Read` 가 누적 토큰 사용량을 빠르게 소진 — Max 5x 5시간 윈도우 조기 도달 문제. Serena MCP = LSP 기반 시맨틱 탐색으로 심볼 단위 부분 읽기 → 광역 grep 대비 토큰 절감. 등록 명령 = `claude mcp add serena-heries -- uvx --from git+https://github.com/oraios/serena serena start-mcp-server --context ide-assistant --project /Users/hongdosan/onion-workspace/heries` (이름 `serena-heries` 로 분리 — 다른 프로젝트 (예: cheongiman) 와 충돌 회피). 사전 조건 = uv 0.11.7 설치 확인 (이미 설치됨). 검증: `claude mcp list` → `serena-heries: ✓ Connected`. CLAUDE.md §도구 우선순위 갱신 = (a) 표 첫 행에 *코드 심볼 탐색 (클래스·함수·변수)* = Serena `find_symbol` / `get_symbols_overview` / `find_referencing_symbols` 추가 (b) 기존 *코드/문서 검색* 행은 *텍스트 패턴 검색 (마크다운 본문·고지문 등 비-심볼)* 로 명확화 (c) 파일 읽기 행에 *코드 파일은 Serena 로 심볼 단위 부분 읽기 우선 — 전체 Read 는 토큰 비용 큼* 단서 추가 (d) 표 하단 §Serena MCP 사용 지침 4 항 신설: src/ ts/tsx 우선 / 사용 예 (find_symbol·find_referencing_symbols·get_symbols_overview) / Grep 은 텍스트 패턴 명확 시 / content·handoff·harness 등 비-코드는 기존 도구. 첫 LSP 인덱싱 5~15분 소요 (백그라운드). 본 세션은 등록 직후라 도구가 노출 안 됨 (mid-session 등록 MCP 는 다음 세션부터 로드 — 정상) — 검증 (심볼 검색 테스트) 은 다음 세션에서 수행 | `~/.claude.json` (project: heries 의 mcpServers 에 serena-heries 항목), `.claude/CLAUDE.md` (§도구 우선순위 표 + §Serena MCP 사용 지침 신설) | 본 시리즈 첫 *MCP 도구 도입* 사이클. 토큰 예산 절감의 잠재 효과 = src/ 의 22 .tsx + .ts 파일에서 심볼 단위 부분 읽기 → 전체 Read 회피. 단, 코드 베이스 자체가 작아 (FSD 6 레이어, 라이브러리 의존 0) 절감 효과는 측정 후 확인 필요. 비-코드 영역 (content/ 마크다운·handoff/·harness/) 은 Serena 미사용 — Read/Grep 유지. 사용자 메모리 *main 브랜치만 + 사용자 직접 commit* 정책과 직교 — 본 변경은 도구 레이어, commit 정책 무영향. 변경 이력 hot 10행 / 한도 20 — 여유 10 |
| 2026-05-12 | **작품 컨셉 전면 개편 — 차용 IP 폐기 + 자작 오리지널 + git history rewrite + 저작권 강한 표기** — 사용자 결정 = *비상업 항변으로도 안전 X, 컨셉 자체를 자작으로 전환 + 저작권 가장 강한 것 (All Rights Reserved)*. **Phase 0 — git history 청소**: (a) 영구 백업 `/Users/hongdosan/onion-workspace/heries-archive/clash-of-multiverses` (1.7 MB), (b) brew install git-filter-repo, (c) uncommitted 10 건 stash, (d) `git filter-repo --path content/series/clash-of-multiverses --invert-paths --force` (42 commits rewrite, HEAD = 06f1047), (e) origin remote 재설정, (f) force push GitHub (`9acf67e → 06f1047 main -> main (forced update)`), (g) stash pop (충돌 0), (h) 백업에서 working copy 복원 (`content/series/clash-of-multiverses/` untracked), (i) `.gitignore` 에 `content/series/clash-of-multiverses/` 추가 (legacy 작가 참조 보존 + 추적 차단). GitHub history 에서 차용 콘텐츠 완전 제거 확인 (`git log -- content/series/clash-of-multiverses` 빈 결과). **Phase 5 — 저작권 강한 표기 일괄 적용**: (a) 모든 .md 19 파일의 `<!-- Fan Fiction · Non-Commercial · Original characters © respective creators -->` → `<!-- © 2026 hongdosan. All rights reserved. Original creator work. -->` 일괄 sed 치환 + harness-install.md grep 예시 명령도 갱신, (b) `LICENSE` content/ scope = 나무위키 CC BY-NC-SA 인용 절 폐기 + *© 2026 hongdosan. All rights reserved.* + 기계학습 데이터 사용 금지 명시 + permission requests 이메일, (c) `NOTICE.md` 전면 재작성 = 14 IP 일람 폐기 + *오리지널 창작 100%* + 외부 IP 차용 ZERO 정책 + 라이선스 문의 채널, (d) `README.md` §비상업적 팬픽 고지 → §저작권 (오리지널 + 외부 IP 차용 ZERO + 코드 MIT 구분), (e) `content/about.md` 전면 재작성 = 오리지널 다세계관 + 자주 받는 질문 (장르 원형 vs 차용 구분), (f) `src/widgets/footer/footer.tsx` disclaimer = *© 2026 hongdosan. All rights reserved.* + 외부 IP 차용 ZERO + 기계학습 데이터 사용 금지 + 라이선스 문의 채널, (g) `src/pages/character/character.tsx` `.character-rights-notice` aside 제거 (필요 X — 모든 캐릭터가 자작), (h) `.claude/CLAUDE.md` 도입 1줄 *비상업 크로스팬픽* → *오리지널 다세계관*, 원칙 #1 *비상업 팬픽 고지* → *저작권 고지*, 원칙 #2 *origin 차용 추적* → *origin 모두 original + 외부 IP 차용 ZERO 정책* + 일반 명사 (닌자·헌터·정파 등 장르 원형) 만 사용 + 식별 가능한 외부 IP 표현 회피, (i) `content/series.json` = `{"series": []}` zero-state. 검증 = typecheck 0 / build 512ms / dist 정상. 로컬 working copy 의 legacy 디렉토리는 `.gitignore` 로 작가 참조 보존 (백업 영구 + working copy 둘 다 보존, 단 GitHub 노출 0) | `LICENSE`, `NOTICE.md` (전면 재작성), `README.md` (§저작권), `content/about.md` (전면 재작성), `content/series.json` (빈 배열), `src/widgets/footer/footer.tsx`, `src/pages/character/character.tsx` (rights-notice 제거), `.claude/CLAUDE.md` (원칙 #1·#2 + 도입 1줄), `.gitignore` (legacy 경로), 모든 .md 19 파일 (저작권 고지 1줄 치환), `.claude/harness/harness-install.md` (grep 예시 명령) | 본 시리즈 *최대 단일 사이클*. 차용 컨셉 (14 IP 크로스오버) 의 근본 위험을 *컨셉 자체 폐기* 로 해소. 이전 사이클 (저작권 빡센 관리 4 레이어 = NOTICE/footer/about/character) 변경사항은 stash → 자작 컨셉에 맞춰 재작성 (NOTICE 14 IP 일람 폐기, character rights-notice 제거 등). 다음 사이클 = **Phase 2 — 10 인 자작 캐릭터 매핑** (사용자 키워드 = 닌자·혈교·천마신교·사파·정파·사대악인·회귀자·환생자·판타지 능력자 9 + *죽은 자 살리는 헌터 (주인공, 시스템 영향자)* + *시스템 영향자 헌터* — 9 키워드 + 주인공 + 추가 1 인 = 10~11 인) → 자작 캐릭터 카드 신규 작성 (`heries-lorekeeper` + `heries-worldsmith` 협업) → 기존 ep-01~04 본문 등장인물·세계관 표현 일괄 치환 (`heries-author` → `heries-continuity-reviewer`) → SSOT 재구성. 변경 이력 hot 15행 / 한도 20 — 여유 5 |
| 2026-05-12 | **저작권 빡센 관리 — NOTICE.md 신설 + 14 IP 일람 + 게재 중단 SLA 명문화 + UI 영구 고지 + CLAUDE.md 원칙 강화** — 사용자 추가 진단 = *비상업이라도 캐릭터 자체 사용에서 권리자 문제 가능, 빡세게 관리*. 진단: 한국 저작권법에서 비상업 팬픽은 *친고죄 묵인 영역* — 권리자 신고 시 침해 인정 가능. 일본 IP (원피스·나루토) 권리 행사 더 엄격. 빡센 관리 = *권리자 발견 즉시 응대 + 침해 의도 없음 입증할 흔적을 모든 레이어에 박기*. 4 레이어 적용: (1) **NOTICE.md 신규** (루트, `LICENSE` 옆) — §차용 작품 일람 (14 IP × 작품·글 작가·그림 작가·연재처·비고 5 컬럼 표) + §게재 중단 요청 (무조건 응대 원칙 = *적법성 항변 없음* + SLA = 24h 수신 확인 / 72h 제거 / 24h 회신 + 이메일 `contact_hongdosan@naver.com` + GitHub Issue 채널 + 익명 신고 접수 + 처리 후 흔적 (NOTICE 1줄 + history rewrite 옵션)) + §비상업 약속 (광고·후원·유료·상품화·데이터 판매 5 영역 None 명시) + §면책 (원작 분리 + 분기 multiverse drift + 오해 금지). 14 IP = 나혼렙·일타강사 백사부·신의 탑·노블레스·화산귀환·절대회귀·갓오하·일렉시드·전독시·광마회귀·초인의 시대·원피스·외모지상주의·나루토 (각 카드 origin 필드에서 추출). (2) **UI 영구 고지 강화** = (a) `widgets/footer/footer.tsx` disclaimer 3 문장 → 5 문장 강화 + *모든 차용 캐릭터의 권리는 각 원저작자에게 귀속* `<strong>` + *72 시간 이내 제거 SLA* 명시 + NOTICE 링크 추가, footer-links 에 *저작권 · 게재 중단* 외부 링크 (b) `pages/character/character.tsx` hero 에 `.character-rights-notice` 추가 — *본 캐릭터의 모든 권리는 원저작자에게 귀속됩니다. 본 페이지는 비상업적 팬 분기이며, 권리자 요청 시 즉시 제거됩니다 — NOTICE.* 모든 22 캐릭터 페이지 영구 표시 (c) `style.css` `.character-rights-notice` 절 (배경 + 좌측 강조선 + 작은 글씨 + 색 톤 다운). (3) **about 페이지 강화** (`content/about.md`) = 3 약속 절 *저작권 안전선* 항목 강화 (NOTICE 링크 + *적법성 항변 없이 무조건 응한다*) + 신규 §권리자께 — 게재 중단 요청 절 (이메일·GitHub Issue 2 채널 + SLA 1 줄 + NOTICE 링크). (4) **CLAUDE.md 원칙 #2 강화** = (a) `origin` 필드 3 요소 필수 (작품명+글 작가+연재처) (b) 신규 *저작권 빡센 관리* 하위 항목 = 신규 IP 차용 시 NOTICE 표 1 행 추가 강제 + 권리자 공식 채널 팬 활동 제한 표명 점검 + 적법성 항변 없이 72h 제거. (5) **README §비상업적 팬픽 고지** 절에 NOTICE 링크 1줄 추가. 검증 = typecheck 0 / build 485ms 성공 (10 verbatim, 29 masked). NOTICE.md 자체는 dist 미복사 (루트 산출물) — footer/about/character 링크는 `https://github.com/hongdosan/heries/blob/main/NOTICE.md` 외부 URL 로 GitHub 단일 SSOT 유지 | `NOTICE.md` (신규, 138 줄), `src/widgets/footer/footer.tsx` (disclaimer 강화 + NOTICE 링크), `src/pages/character/character.tsx` (hero 권리 고지 추가), `src/shared/styles/style.css` (`.character-rights-notice` 절 신규), `content/about.md` (§저작권 안전선 + §권리자께 절 강화), `README.md` (NOTICE 링크 1줄), `.claude/CLAUDE.md` (원칙 #2 — origin 3 요소 + 저작권 빡센 관리 하위 항목) | 사용자 명시 = *수익 없더라도 캐릭터 사용 자체가 문제될 수 있으니 빡세게 관리*. 본 사이클 = 즉시 적용 가능 4 레이어 (NOTICE / UI 영구 고지 / about / CLAUDE) 완료. 보류 항목 = (1) robots.txt noindex (검색 노출 차단 — 사용자 의도 확인 필요) (2) 22 카드 표 셀 단위 재작성 (lorekeeper 점진) (3) sitewide 권리자 공식 채널 팬 활동 표명 점검 (14 IP 사용자 직접). 본 변경 이후 신규 IP 차용 시 NOTICE 표 갱신 + origin 3 요소 강제 — 저작권 SSOT 단일화. 변경 이력 hot 14행 / 한도 20 — 여유 6 |
| 2026-05-12 | **나무위키 인용 라이선스 정합 (LICENSE + README 즉시 보완)** — 사용자 진단 = *모든 등장인물 카드 22장이 나무위키 본문 기반*. 위험 평가: (1) 나무위키 본문 라이선스 = **CC BY-NC-SA 2.0 KR** (NC ✅ heries 비상업 / BY ❌ 출처 표시 / SA ❌ 동일 라이선스 전파 미명시). (2) 원작자 권리 (별개) = 캐릭터 사실 정리 자체가 원저작자 권리 영역. 카드 형태가 표·짧은 bullet 구조 (장문 산문 복붙 X) 라 *완화 요소* 있으나 표 셀의 짧은 표현 잔재 + 항목 분류 자체에 표현 저작권 우려. **즉시 적용** (옵션 A + C): (a) **LICENSE 정정** — `Copyright (c) 2026 hong` → `hongdosan` (사용자 메모리 `user_author_name` 정합) + content/ scope 절에 *나무위키 (CC BY-NC-SA 2.0 KR) 인용 및 SA 호환 CC BY-NC-SA 4.0 배포* 단락 추가 (b) **README §비상업적 팬픽 고지** 강화 — *원작 권리자 게재 중단 요청 시 즉시 응한다 (contact_hongdosan@naver.com / GitHub Issue)* 연락처 명시 + *등장인물 카드의 원작 사실 정리는 나무위키 (CC BY-NC-SA 2.0 KR) 를 참고했으며 content/ 산출물은 CC BY-NC-SA 4.0 로 배포한다* 1줄 추가. (c) 카드 각 §검증 출처 절은 기존 형태 유지 — 마스킹 정책 (CLAUDE.md §9) 이 *§검증 출처 절은 reader 빌드 유지* 로 이미 보장. **중기 (옵션 B) 보류** = 22 카드 표 셀 단위 재작성은 챕터 작성 사이클 중 lorekeeper 가 *터치하는 카드 한정으로 점진 재작성* (사용자 메모리 `feedback_progressive_sync` 정합). 검증 = LICENSE/README 1회 Read 확인, typecheck/build 영향 없음 (content 산출물 변경 X) | `LICENSE` (Copyright 정정 + content/ scope 절 나무위키 인용 단락 추가), `README.md` (§비상업적 팬픽 고지 절 4 → 5 항목, 연락처 + 나무위키 인용 1줄) | 본 시리즈 첫 *콘텐츠 라이선스 정합* 사이클. 사용자 메모리 직접 변경 없음 — `user_author_name` (hong 폐기) 정합 + `feedback_progressive_sync` 정합 (B 옵션 보류). 향후 lorekeeper 가 카드 터치 시 §검증 출처 절 형식 유지 + 의심 표현 재작성을 동시 수행하는 정책 = 다음 사이클 진화 후보. ep-05 작성 시점에 진모리·청명 (이미 등장) 카드부터 정합 작업 자연 발생 예상. 변경 이력 hot 13행 / 한도 20 — 여유 7 |
| 2026-05-12 | **11 인 자작 캐릭터 매핑 초안 확장 — 11번째 인물 *백무진 (카피 능력자)* 추가** — 사용자 추가 요청 = *일반적인 무능력자인데 유일한 능력이 카피인 등장인물도 하나 넣자*. 본 시리즈 신규 11 번째 인물 자작 명세: 이름 *백무진 (白茂進, "백지에서 무성히 나아간다")* / 진영 *무명자 — 차원·문파·시스템 무소속 일반인* / 핵심 능력 *심상 모사 (心象摹寫) — 한 번이라도 의도·구조까지 깊이 인지한 능력을 본인 심상으로 모사, 본인 기량 한계 내 발현, 발동 조건 = 접촉이 아닌 관찰·이해* / 페이즈 1 역할 = *최약·관찰자·최후 생존자, 강자들이 서로 소모할 동안 지켜보며 살아남음, 페이즈 2 떡밥 = 모든 능력을 본 자 → 작가가 그를 역설계*. **외부 IP 식별 점검** = 접촉 발동·복제 카드·진화형 (모노마·가로우 등) 회피, *심상 모사 + 깊은 인지·기량 한계* 로 차별화. CURRENT.md 4 위치 갱신 = (a) Summary 끝 *10 인 → 11 인* (b) §Phase 2 헤딩 *10 → 11 인* + 키워드 목록 *무능력자·카피* 추가 (c) 매핑 표 11 번째 행 추가 (d) 외부 IP 식별 점검 절에 카피·무능력자 항목 추가 + 다음 단계 *10 인 카드 → 11 인 카드* | `.claude/handoff/CURRENT.md` (4 위치 갱신) | 본 행 = 사용자 검토 *그대로 가자* 후 매핑 표 1차 확정 사이클. 다음 진입 단계 = *heries-lorekeeper + heries-worldsmith 협업으로 11 인 카드 본문 작성*. 매핑 → 카드 본문 → 챕터 본문 자작 변환 3 단계 중 1 단계 확정. 변경 이력 hot 16행 / 한도 20 — 여유 4 |
| 2026-05-12 | **11 인 매핑 정정 — 백무진 카피 한계 명시 + 시스템 헌터 비-한국 여성 (에이라) + 회귀자 종족 다양화 (조상 혈통 잔류)** — 사용자 명시 3 결정: (1) *형태는 인간에서 벗어나지 않음, 다른 종족이더라도 인간 모습* (2) *카피 능력자는 능력 자체까지는 카피 불가, 그들의 기술만 카피 가능* (3) *보통 한국인이 다수 + 서한 → 에이라 (여)*. CURRENT.md 매핑 표 3 행 정정: (a) #11 백무진 능력 = *심상 모사* → *기예 모사 (技藝摹寫)*, 모방 대상 = **기술 (수련 기예 — 검술·도법·결인술·암기·체술)** 만, **시스템 능력·체질·이능 (시간 회귀·전생 본능·각인 룬·시스템 영향·고대 종족 특질) 자체는 모방 불가** 명시, 페이즈 2 떡밥 *모든 기술을 본 자 (능력 자체 X, 기예 O)* 로 정정. (b) #2 *서한 (徐瀚)* → ***에이라 (Eira) 여*** + 진영 *북방 헌터 길드 출신 (비-한국, 북유럽풍)* 명시. (c) #9 림우경 = 한국식 한자명 + 한국 일반인 외형 유지 + *조상 중 1 인이 멸망한 고대 차원의 마지막 후예 (혈통 잔류, 외형 완전 인간)* 설정 추가 + 능력 메커니즘 = **초감각 시간 인지 (10⁻⁹ 초 = 나노초 단위 시간 감각)** 발현 + 수 초 시간 역행 (회귀). 외부 IP 점검 절 갱신 = 회귀자 *고대 차원 혈통 + 나노초 인지* + 헌터 *에이라* 추가 점검. 한국인 비율 = 한국 9 + 일본 닌자 1 + 비-한국 헌터 1 = *다수 한국인 유지*. 종족 다양화는 *조상 혈통 수준* 으로 한정 (외형 완전 인간 정책 정합) | `.claude/handoff/CURRENT.md` (3 행 정정 + 외부 IP 점검 절) | 본 사이클 = 캐릭터 매핑 *세부 조율 1차*. 다음 진입 단계 = *heries-lorekeeper + heries-worldsmith 협업으로 11 인 카드 본문 작성* 또는 추가 매핑 조정. 백무진 카피 한계 명시는 향후 카드 본문 작성 시 *능력 vs 기술* 구분이 명확해야 함 (lorekeeper §능력 절에서 분리 작성). 변경 이력 hot 17행 / 한도 20 — 여유 3 |
| 2026-05-12 | **11 인 자작 카드 본문 1차 패스 + manifest 등재 + 이미지 압축 + 2 분리 commit** — heries-orchestrator skill 호출 → general-purpose subagent (model: opus) 통해 lorekeeper agent 정의 inline 위임 (Agent subagent_type 'heries-lorekeeper' 미등록으로 우회). 본 사이클 산출: (1) **11 카드 신규** = `content/series/clash-of-multiverses/characters/1-protagonist/hyeon-woo-jin.md` + `2-major-supporting/{eira, yamura-tou, seo-un-hyeok, ryeong-geuk, go-cheon-han, yu-baek-gyeong, bi-hyeon, rim-woo-gyeong, hyeon-woo, baek-mu-jin}.md`. (2) **manifest.json** characters 배열 11 인 등재 (status: 재구축 중 유지). (3) **이미지 압축** = `content/_shared/{heries-mark, thumbnail-placeholder}.webp` 4302 → 295 KB (-93%), 빌드 게이트 500 KB/이미지 통과. (4) **2 분리 commit** = `dd7f170` chore(images) 이미지 + `09c35bb` feat(characters) 11 카드 + manifest. **검증**: (a) 카드 평균 53 줄 (50~67 범위, 백무진 67 = 모방 가능/불가 분리 표) (b) 외부 IP grep 점검 6 항목 모두 0 매칭 (그림자 군주·차크라·매화검법·수라혈천도·접촉 발동·모노마 등) (c) 모든 카드 `origin: original` + 저작권 고지 1줄 + 공개 절·작가 분기 절 분리 + heries_arc 마스킹 필드 (d) 핵심 정정 일관 = 백무진 *기예 모사* (능력 자체 X, 기술만 O) / 림우경 한국 외형 + 조상 혈통 작가 분기 마스킹 / 에이라 비-한국 여성 / 인간형 외형 필수. **typecheck/build 실패** = 본 사이클 무관 환경 이슈 (node_modules/vite 미설치 + `npm run typecheck` 의 `tsc -b --noEmit` 충돌 TS5094) — frontend-engineer 별도 사이클 처리 권장 | `content/series/clash-of-multiverses/characters/{1-protagonist,2-major-supporting}/*.md` (11 신규), `content/series/clash-of-multiverses/manifest.json` (11 인), `content/_shared/{heries-mark,thumbnail-placeholder}.webp` (압축) | 본 사이클 = 작품 컨셉 전면 개편 (2026-05-12) 후 *첫 카드 본문 작성 사이클*. lorekeeper 위임 패턴 = orchestrator skill 라우팅 + general-purpose subagent inline lorekeeper 정의 (Agent type 미등록 우회) 검증 완료 — 다음 사이클부터 동일 패턴 적용. 다음 사이클 후보 = (a) worldsmith 진영 SSOT 11 본문 작성 + `_series.md` 10 → 11 인 동기화 (b) 추가 결정 4 항목 (진영 간 관계 그리드 / 페이즈 2 작가 정체 단서 일관성 6 단서 수렴 여부 / 현우진-에이라 동맹 or 라이벌 / 백무진 *모든 기술 본 자* 떡밥 페이즈 2 발현 형식) (c) frontend-engineer 환경 복구 (`npm install` + typecheck 스크립트 정정) (d) ep-01~04 자작 변환 (legacy 본문에서 사건·구성·문체 보존, 등장인물·세계관 표현만 자작 치환). 변경 이력 hot 18행 / 한도 20 — 여유 2, 다음 사이클 후 임계 임박 보고 필요 |
| 2026-05-12 | **대규모 통합 사이클 — orphan squash (history 초기화) + SPA 전환 + NOTICE 페이지 + 옵션 C 11 카드 fix + 라이선스 All Rights Reserved 통일 + ZERO 정책 표현 통일** — 사용자 결정 5건: (1) *원격 GitHub commit 기록 다 초기화 (병렬)* (2) *SPA 로 진행* (3) *코드도 저작권 가장 강한 것* (4) *NOTICE 따로 페이지 + 전체 프론트엔드 개선* (5) 옵션 C (11 카드 렌더링 차단 fix). 단일 메가 사이클 산출: (a) **git history rewrite** = orphan branch + root commit `e394480` + force push origin main. 이전 39 commit (e00ba18 부터, *non-commercial cross-fanfic·sungjinwoo·14-original list·spoiler masking* 등 차용 컨셉 흔적 다수) → root 1 commit squash. GitHub reachable history 차용 흔적 0. dangling commit 은 자동 gc 대기 (수 주~수 개월) — 사용자 결정 *repo 재생성 안 함*. (b) **SPA 전환** (`b68a233`) = HashRouter → BrowserRouter + basename `import.meta.env.BASE_URL.replace(/\/$/, '')` + vite.config base 분기 (production `/heries/` / dev `/`) + publicDir 활성화 + public/404.html 신규 (spa-github-pages redirect) + index.html replaceState 복원 inline script + meta description 갱신. (c) **NOTICE 페이지** = `src/pages/notice/{notice.tsx,index.ts}` 신규 + `<Route path="/notice">` 등록 + footer NOTICE 외부 GitHub URL → 내부 `<Link to="/notice">` 전환 (2곳) + `content/notice.md` dev 미러 + `scripts/copy-content.mjs` 가 build 시 루트 NOTICE.md → dist/content/notice.md 자동 복사 (SSOT = 루트 NOTICE.md). (d) **환경 복구** = `npm install` 73 packages 0 vulnerabilities + `package.json` typecheck `tsc -b --noEmit` → `tsc --noEmit` (TS5094 충돌 제거). (e) **footer `.site-footer .meta` CSS 정의 8행 제거**. (f) **옵션 C 11 카드 fix** (`1e3aa79`) = manifest.json characters `string[]` → `CharacterIndex[]` 객체 배열 (11 인 `{id, folder, name}`) + types.ts CharacterFolder union 에 `'1-protagonist'` 추가 (legacy `'1-main-character'` 보존) + manifest.ts `normalizeSeriesManifest` + `normalizeCharacters` 헬퍼 (string[] 레거시도 graceful) + character.tsx FOLDER_LABEL `'1-protagonist': '주인공'` + character-list.tsx GROUP_ORDER 선두 추가. (g) **라이선스 통일** (`736d12e`) = LICENSE MIT scope 폐기 → 전체 단일 All Rights Reserved + NOTICE §코드 라이선스 (구분)·§이력 절 제거 + README §저작권 *코드 MIT 별도* 제거. (h) **표현 통일** (`b73ff5c`) = *외부 IP 차용 0* → *외부 IP 차용 ZERO 정책* (NOTICE/content/notice/CLAUDE.md/CURRENT.md/footer disclaimer/state.md 일관) + README HashRouter 잔존 3 곳 → BrowserRouter + content/about.md FAQ heading 형식 (`**Q.**` → `### Q.` 사용자 직접). (i) **scripts/copy-content.mjs (`0b2adf4`)** = 미사용 `stat` import 제거 (NOOP cleanup). **commit** = root `e394480` + 6 commit (`b68a233` SPA + `07756b0` footer 링크 + `9cfe194` NOTICE 표현 + `1e3aa79` 옵션 C + `0b2adf4` stat 정리 + `b73ff5c` 문서 표현 통일) + 사전 3 commit (`736d12e` 라이선스 + `4b04113` footer meta + `6c3d3bf` 핸드오프 — orphan squash 전, root commit 에 흡수). **검증**: typecheck 0 / build 1.11s 0 에러 / 11 카드 dist 정상 + loadCharacter find 11/11 매칭 / 마스킹 누수 0 (heries 분기 0건, heries_arc 0건) / FSD 위반 0 / SPA 라우팅 흐름 GitHub Pages 호환 (404.html → ?/path → replaceState → React Router 매칭) | **메가 사이클** = root `e394480` 99 files + 6 후속 commit. 주요 = LICENSE / NOTICE.md / README.md / content/notice.md / content/about.md / .claude/CLAUDE.md / .claude/handoff/CURRENT.md / .claude/harness/harness-state.md / content/series/clash-of-multiverses/manifest.json / src/app/main.tsx / src/pages/notice/{notice.tsx,index.ts} / src/widgets/footer/footer.tsx / src/shared/lib/{types,manifest}.ts / src/pages/character/character.tsx / src/widgets/character-list/character-list.tsx / src/shared/styles/style.css / scripts/copy-content.mjs / vite.config.ts / index.html / public/404.html / package.json | 본 사이클 = heries 최대 통합 사이클. *원격 history 초기화* 가장 destructive (사용자 명시 권한). agent 위임 패턴 추가 검증 = (1) lorekeeper (옵션 C 콘텐츠) + frontend (옵션 C 코드) 병렬 분리 충돌 0 (2) agent 미커밋 stash + orphan checkout + stash pop (3) Agent type 미등록 → general-purpose + agent 정의 inline 우회 안정. **잔여 결정** = (a) GitHub UI repo 재생성 보류 (자동 gc 대기) (b) 11 카드 추가 SSOT 4 항목 (진영 간 관계 / 페이즈 2 작가 정체 6 단서 / 현우진-에이라 / 백무진 떡밥 발현) 보류. **다음 사이클 후보** = (1) worldsmith 진영 SSOT 11 본문 + _series.md 10 → 11 인 동기화 (2) build:author base 분기 (`--base=/`) (3) ep-01~04 자작 변환. 변경 이력 hot **19행 / 한도 20 — 여유 1**. **다음 사이클 시작 직후 압축 임계 도달 (20행) — archive 압축 발동 의무** |
| 2026-05-12 | **메가 사이클 후속 마무리 — 이미지 재압축 + favicon 교체 + 세레나 재등록 + ErrorBoundary + build:author base 분기 + 프론트 정리** — 사용자 명시 5건: (1) *이미지 압축* (2) *favicon = heries-mark 로 설정 / 기존 차원의 격돌 이미지 어디?* (3) *세레나 연동* (4) *전체 프론트엔드 검토 + 에러 바운더리* (5) *알아서 커밋·푸시*. 단일 후속 마무리 사이클: (a) **이미지 재압축** (`f9fbcf2`) = _shared 2장 = heries-mark 2248 → 91 KB (-96%, 사용자 신규 이미지) + thumbnail-placeholder 230 → 220 KB (-5%) = 합 2478 → 311 KB (-87%). (b) **favicon 교체** (`914cba8`) = `src/shared/img/favicon.webp` 를 heries-mark.webp (보라색 h 마크 + 다중우주 모티프) 로 덮어쓰기. (c) **세레나 MCP 재등록** = `claude mcp add serena-heries` 경로 `/Users/hongyeongjune/IdeaProjects/heries` (이전 stale 경로 `/Users/hongdosan/onion-workspace/heries` 폐기, 시스템 username 정정). 검증 `serena-heries: ✓ Connected`. 첫 LSP 인덱싱 5~15분 백그라운드 → 다음 세션부터 도구 가용. (d) **ErrorBoundary 전역 도입** (`1f351d8`) = `src/shared/ui/error-boundary/{error-boundary.tsx, index.ts}` 신규 (React 19 Class Component 직접 구현, 의존성 0 정책 = react-error-boundary 미사용). `src/app/main.tsx` 의 `<BrowserRouter>` 내부 `<Header>` 와 `<Routes>` 사이 전역 wrap (옵션 A). fallback 분기 = dev (`import.meta.env.DEV`) → error.message + stack + component stack `<details open>` / production → "문제가 발생했습니다" + 사용자 친화. 둘 다 console.error. 「홈으로」 = `window.location.assign(import.meta.env.BASE_URL)` (basename 정합). CSS `.error-boundary` 절 신규. (e) **build:author base 분기** (`6d7bcd9`) = `vite.config.ts` 에 `VITE_AUTHOR_MODE` env 분기 = reader build production `/heries/` / author build `/` / dev `/`. dist-author/index.html asset 경로 `/assets/...` (no /heries) → `npx serve dist-author` 즉시 동작. dist (reader) 는 `/heries/assets/...` 유지. agent 2 이전 부차 발견 해소. (f) **프론트엔드 정리** (`6c7c4db`) = `pages/chapter/chapter.tsx:2` import 명시 (`'../../entities/chapter' → '../../entities/chapter/index.js'`, FSD 일관성) + `widgets/header/header.tsx` theme-toggle aria-label + title 추가 (a11y, accessible name 누락 정정) + `style.css` stale class 4 영역 삭제 (`.theme-toggle-text` × 2 / `.header-contact-arrow` × 2 / `.character-rights-notice` × 2 / `.sr-only`) + undefined var 정정 (`--bg-2` → `--bg-soft` / `--rule-soft` → `--rule` / `--shadow-1` fallback → `--shadow`). **검증**: typecheck 0 / reader build 0 에러 + 마스킹 누수 0 (9 verbatim, 12 masked) / author build 0 에러 (24 verbatim, 0 masked) / check-images OK / FSD 위반 0 / 마크다운 렌더러 11+ 케이스 통과. **legacy 차원의 격돌 이미지 복구 불가** = dangling commit·blob 어디에도 cover.webp / ep-01~04 thumbnails 없음 (직전 main `6c3d3bf` tree 도 빈 상태 = orphan squash 이전부터 이미 제거). 복구 가능 자산 = unreachable PNG 1개 (`be9b36f` 1254×1254 RGBA 2.3 MB) — 사용자 결정 보류. 백업 경로 `/Users/hongdosan/onion-workspace/heries-archive/` 도 stale (시스템 username `hongyeongjune` vs 작가명 `hongdosan` 혼동의 결과 추정). **commit** = 5 분리 (f9fbcf2 이미지 재압축 + 914cba8 favicon + 1f351d8 ErrorBoundary + 6d7bcd9 base 분기 + 6c7c4db 프론트 정리) | `src/shared/ui/error-boundary/{error-boundary.tsx,index.ts}` (신규), `src/app/main.tsx` (ErrorBoundary wrap), `vite.config.ts` (VITE_AUTHOR_MODE base 분기), `src/widgets/header/header.tsx` (a11y), `src/pages/chapter/chapter.tsx` (FSD), `src/shared/styles/style.css` (stale class + undefined var + .error-boundary), `src/shared/img/favicon.webp` (heries-mark 교체), `content/_shared/{heries-mark,thumbnail-placeholder}.webp` (재압축), `~/.claude.json` (serena-heries 등록), `.claude/handoff/CURRENT.md` (덮어쓰기), `.claude/harness/harness-state.md` (본 행) | 본 사이클 = 메가 사이클 (직전 통합) 후속 마무리. **agent 위임 패턴 4회 검증** = lorekeeper × 2 + frontend-engineer × 4 모두 general-purpose inline 우회로 안정. ErrorBoundary 는 사이트 전역 에러 격리 첫 도입 — 향후 페이지별 wrap 추가 시 더 세밀한 격리. build:author base 분기로 *작가 로컬 검수 워크플로우* 가 npx serve / file:// 등 다양한 환경 호환. **legacy 이미지 복구 불가 = 작품 컨셉 전환 사이클의 누락** — 다음 사이클부터 *백업 경로 점검 strict + 정책 SSOT 유지* 필요. **다음 사이클 후보** = (1) 변경 이력 archive 압축 (hot 20 도달 = 필수 발동) (2) worldsmith 진영 SSOT 11 본문 + `_series.md` 10 → 11 인 동기화 (3) ep-01~04 자작 변환 (4) frontend 부차 발견 항목 (useAsync AbortController·ChapterPage 분해·페이지별 ErrorBoundary·외부 링크 rel) (5) legacy dangling PNG `be9b36f` 복구 결정 (6) 천기망 Serena MCP (별도 프로젝트, 절대 경로 미회신). 변경 이력 hot **20행 / 한도 20 — 도달**. **다음 사이클 시작 직후 archive 압축 발동 의무** |
| 2026-05-11 | **챕터 UX 3종 + ep-03·ep-04 작성 + 수라혈천도 카드 SSOT 정정 + 카드 경로 일괄 정정** — 한 세션 누적 다중 사이클: (1) **챕터 UX 3종** — chapter-toc.tsx 정렬 토글 (최신순/연재순 pill) + chapter.tsx outline TOC (`<details>` + H2 자동 추출 + smooth scrollIntoView, HashRouter 충돌 회피로 hash 변경 X) + 우측 하단 ↑↓ 스크롤 nav (color-mix + backdrop-filter, 모바일 위치 보정). renderMarkdown 에 heading id 자동 부여 + `extractOutline()` export. (2) **렌더러 깨짐 2종 수정** — URL 안 균형 `()` 한 단계 허용 (namu.wiki/w/foo(웹툰) 류 8건 영향) + italic 정규식 lookbehind 안정화 (CommonMark 룰 일치) + bam.md 중첩 italic 1건. (3) **ep-03 *2막 — 혼돈, 다섯 명의 추락* 작성** — 시놉시스 보존 + 청명/카이든/밤/김독자/백수룡 5 추락 + 자체 검토 14 보정 (잔해 재폭락 / 잊혀진 → 잊힌 / 예토전생 메타 제거 / 청명 *거 시끄럽다* → *쯧, 정신 사납네* (ep-02 반복 회피) 등). (4) **백수룡 카드 수라혈천도 출처 정정** — 사용자 지적: 수라혈천도는 *광마회귀(이자하)* 가 아니라 *일타강사 백사부* 의 *광마 헌원후* (백수룡 네 사부) 가 헌원세가 가전 *파천도* 기반 백인 비무행에서 정립한 절세도법. ep-03 본문 line 304 출처 정정 + baek-su-ryong.md §무공 표 *무공/사부·출처/비고* 3열 재편 + 네 사부 4인 모두 확정 (검존 모용혼·광마 헌원후·빙월신녀 은예린 신규 + 기존 맹호악). 사용자 직접 *namu.wiki 네 사부 무공* 본문 페이스트로 보강. (5) **카드 전체 _series.md 상대 경로 일괄 정정** — 15 카드 × 2 occurrence (총 30건) `../_series.md` → `../../_series.md` (sed 일괄). IDE diagnostic 만 영향 (SPA 렌더링 무관). (6) **ep-04 *3막 — 그림자 군주, 일어선 죽은 자들* 작성** — 시놉시스 보존 (성진우 *일어나라* → 죽은 8 그림자 도열 → 진모리 화안금정 *위험* 본능 → 임강 본체 직격 → 그림자 교환 → 검무극 *본체 노려야* 협공 → 5인 임시 휴전 돌진) + 자체 검토 12 보정 (예토전생 → 환술 / 부러지지 않는 신념 + 책 *멸살법* 분리 / 임강 이름 X / 어깨 → 전신 / 시그니처 → 방식 등). PROMPT_REQUEST 도 ep-04 *6:8 대비 + 정지 vs 돌진 구도* 로 전면 갱신. ep-03 진모리 *1막 미동* 묘사 사용자 지적 정정 (1막 여의 거대화 → 밤 신수 짓누름 사실 보존). | `src/widgets/chapter-toc/chapter-toc.tsx` + `src/pages/chapter/chapter.tsx` + `src/shared/styles/style.css` + `src/shared/lib/markdown.ts` (3종 UX + heading id + extractOutline + URL/italic 수정), `content/series/clash-of-multiverses/chapters/{ep-03-chaos-five-falls,ep-04-shadow-monarch-rises}.md` (신규 2 화), `content/series/clash-of-multiverses/manifest.json` (chapters[] +2), `content/series/clash-of-multiverses/thumbnails/PROMPT_REQUEST.md` (ep-03 → ep-04 두 차례 갱신), `content/series/clash-of-multiverses/characters/2-major-supporting/{baek-su-ryong,bam}.md` (사부 4인 보강 + 중첩 italic 수정), `content/series/clash-of-multiverses/characters/**/*.md` (15 카드 30건 경로 정정), 썸네일 4장 일괄 압축 (ep-03 = 3743KB → 208KB -94%) | 본 세션 = 한 사이클당 4~5 분리 커밋 패턴 정착. 시놉시스 사건 보존 원칙 ep-03·ep-04 양화 모두 100% 통과. 카드 SSOT 미해소 영역 (네 사부 4인 중 3인 미상) 도 사용자 직접 자료 페이스트로 해소 — 챕터 작성 사이클이 카드 보강 사이클을 *역동적으로 트리거* 하는 패턴. ep-04 클로징 = *5 vs 1 + 그림자 8 압박* cliffhanger. 다음 사이클 = ep-05 시놉시스 대기 (5인 협공 결과 + 페이즈 1 클로징 또는 페이즈 2 진입). 변경 이력 hot 9행 / 한도 20 — 여유 11 |

> 다음 진화 트리거: `agent-lorekeeper` 시범 생성 후 §에이전트 인벤토리·구성 요소 표 갱신.

## 참고

- 도입 가이드: [harness-setup.md](harness-setup.md)
- 설치·적용: [harness-install.md](harness-install.md)
- harness 저장소: https://github.com/revfactory/harness
- harness SKILL.md: https://github.com/revfactory/harness/blob/main/skills/harness/SKILL.md
